import * as React from 'react';
import { View } from '~components/Views/View';

export const Settings: React.FC<{}> = () => (
    <View>
        <h2>Settings</h2>
        <p>Settings can be controlled here.</p>
    </View>
);
